import { Form } from "@/components/react-components/form";

export default function LoginPage() {
  return <main className="w-full min-h-[100svh] flex items-center justify-center">
    <Form />
  </main>
}